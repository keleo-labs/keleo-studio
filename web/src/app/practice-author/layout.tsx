import type { ReactNode } from "react";

export const metadata = {
  title: "Practice author — Adoption framework",
  description: "Validate, render, and edit practice JSON documents.",
};

export default function PracticeAuthorLayout({ children }: { children: ReactNode }) {
  return children;
}
